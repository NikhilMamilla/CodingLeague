{
  "name": "cwcl-functions",
  "description": "Firebase Cloud Functions for CWCL — currently just the auth blocking function that bridges Firebase Auth into Supabase's Third-Party Auth.",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  },
  "private": true
}
